cleanup_frequency: 600 # Unit: seconds.

tests:
  - test_type: microbenchmark # test type defined in registry.py
    interval: 86400 # Unit: seconds.
    slackbot_update: True # Update the result via slackbot after the test is finished.
    s3_update: True # Update the result via S3 after the test is finished.
    kwargs:
      ray_branch: # Null
      commit: # Null
      ray_version: # Null
